apiVersion: v1
kind: ResourceQuota
metadata:
  name: payments-compute-quota
  namespace: payments
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 4Gi
    limits.cpu: "4"
    limits.memory: 8Gi
