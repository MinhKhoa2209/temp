Cluster: minikube profile w10
Date: 2026-06-18

Valid workload proof:

kubectl get deploy payment-processor -n payments
NAME                READY   UP-TO-DATE   AVAILABLE
payment-processor   2/2     2            2

kubectl apply --dry-run=server -f apps/payments/deployment.yaml
deployment.apps/payment-processor unchanged (server dry run)

Gatekeeper rejection proof:

kubectl run gatekeeper-test --image=nginx:latest -n payments --restart=Never --dry-run=server

Error from server (Forbidden): admission webhook "validation.gatekeeper.sh" denied the request: [disallow-root-user] container <gatekeeper-test> must not run as root
[disallow-latest-tag] container <gatekeeper-test> uses disallowed image tag <latest>
