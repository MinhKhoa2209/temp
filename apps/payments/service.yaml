apiVersion: v1
kind: Service
metadata:
  name: payment-processor
  namespace: payments
  labels:
    app: payment-processor
spec:
  type: ClusterIP
  selector:
    app: payment-processor
  ports:
  - name: http
    port: 80
    targetPort: 8080
